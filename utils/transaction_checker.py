import random

async def check_transaction_on_chain(tx_hash: str, expected_amount: float) -> bool:
    # هذه دالة وهمية يمكنك ربطها بـ TronGrid أو API خاص لاحقًا
    # الآن يتم المحاكاة فقط
    return random.choice([True, False])
